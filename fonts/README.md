# Шрифты сюда НЕ класть

Каталог оставлен как указатель: шрифты живут в **`src/assets/fonts/`** (см.
README там). Из `public/` шрифт не получает immutable-кэш `/_astro/` и
ревалидируется на каждом переходе — два лишних round-trip к
рендер-критичному ресурсу. Канон: `docs/PERFORMANCE.md` §3.
